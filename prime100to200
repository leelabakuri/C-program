#include <stdio.h>
void main()
{
    int i,n,c;
    for(n=100;n<=200;n++)
    {
        c=0;
        for(i=1;i<=n;i++)
        if(n%i==0)
        c++;
        if(c==2)
        printf("%d\n",n);
    }
}
